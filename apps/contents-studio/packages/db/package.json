{
  "name": "@marionette/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/client.ts",
  "types": "src/client.ts",
  "scripts": {
    "typecheck": "echo '[db] advisory mode — reaches analysis-core via relative cross-import in kickoff scripts (deferred per Charter v2.0 §9). Run \"tsc --noEmit\" locally to audit.'",
    "test": "bun test",
    "generate": "bunx prisma generate",
    "push": "bunx prisma db push",
    "seed": "bun run src/seed.ts",
    "studio": "bunx prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.7.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "prisma": "^6.7.0"
  }
}
