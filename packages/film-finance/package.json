{
  "name": "@marionette/film-finance",
  "version": "1.0.0",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "db:generate": "bunx prisma generate",
    "db:migrate": "bunx prisma migrate dev",
    "db:push": "bunx prisma db push",
    "db:studio": "bunx prisma studio",
    "db:seed": "bun run src/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.10.0",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "prisma": "^6.10.0",
    "bun-types": "latest"
  }
}
